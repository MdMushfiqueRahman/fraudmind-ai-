# FraudMind AI — local development only. Copy to .env and fill in.
# NEVER commit .env or any file containing real keys.

# Required for threat analysis (get one at https://platform.openai.com/api-keys)
OPENAI_API_KEY=

# Optional. Set for production; omit for local dev (insecure default used).
# DJANGO_SECRET_KEY=
